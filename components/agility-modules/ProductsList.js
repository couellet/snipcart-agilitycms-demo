const ProductsList = (props) => {
    return (
        <h1>Products list</h1>
    )
};

ProductsList.getCustomInitialProps = async ({
    agility,
    languageCode}) => {
}

export default ProductsList;